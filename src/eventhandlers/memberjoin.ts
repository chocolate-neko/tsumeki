import { Guild, Member } from 'eris';
import { TsumekiClient } from '../client';

export function memberJoin(
    guild: Guild,
    member: Member,
    client: TsumekiClient,
) {
    // TODO Move member join messages to seperate file
}
